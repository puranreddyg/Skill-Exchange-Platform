services:
  - type: web
    name: skill-exchange-monolith
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: ~20
      - key: PORT
        value: 3001
      - key: GEMINI_API_KEY
        sync: false
